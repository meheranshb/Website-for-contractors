[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

# Required for Next.js (SSR, API routes, dynamic pages) on Netlify.
[[plugins]]
  package = "@netlify/plugin-nextjs"
